#include <stdio.h>
typedef struct
{
    int coef;
    int power;
}term;
int main()
{
    int m,n,i;
    
    printf("Enter the number of terms of first polynomial: ");
    scanf("%d",&m);
    term a[m];
    printf("Enter coef and power of each term line by line: ");
    for(i=0;i<m;i++)
    {
        scanf("%d",&term[i].)
    }
    printf("Enter the number of terms of first polynomial: ");
    scanf("%d",&n);
    term b[n];
}