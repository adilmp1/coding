#include <stdio.h>
#include <stdlib.h>
int main()
{
    system("cls");
    int i;
    char str[20];
    printf("Enter the string: ");
    gets(str);
    for(i=0;str[i]!='\0';i++)
    {

    }
    printf("The lenght of the string is %d",i);

}